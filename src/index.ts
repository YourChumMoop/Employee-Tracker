
import Cli from "./cli/Cli.js";
const main = new Cli;

main.mainMenu()
// TODO Main menu inquary, with options for View all dept, view all roles, view all emp, add a dept,rol,or emp, finally update an emp role

// TODO Cases for each selection

// TODO case for View alls viewAll(response)

// TODO case for add dept, role or emp AddDept(name), addRole(name), addEmp() <-This one will have more quary questions

// TODO Update Emp Role with updateEmp() <-Quary to select which employee and what is to be updated